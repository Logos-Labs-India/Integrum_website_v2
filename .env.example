# Copy to .env.local for local overrides. Vite only exposes variables
# prefixed VITE_ to the browser bundle — never put secrets here.

# Base URL of the Integrum lead API (see server/). Defaults to
# http://localhost:4000 in src/leads.js if unset.
VITE_API_BASE_URL=http://localhost:4000
